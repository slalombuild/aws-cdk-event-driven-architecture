import * as cxapi from '@aws-cdk/cx-api';
declare type Options = {
    concurrency: number;
    deployStack: (stack: cxapi.CloudFormationStackArtifact) => Promise<void>;
};
export declare const deployStacks: (stacks: cxapi.CloudFormationStackArtifact[], { concurrency, deployStack }: Options) => Promise<void>;
export {};
